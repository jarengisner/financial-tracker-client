import React from "react";
import { Modal } from "react-bootstrap";

export const PasswordChange: React.FC = ()=>{
  return(
    <Modal>
      <Modal.Body>
        <p>Placeholder for the change password modal</p>
      </Modal.Body>
    </Modal>
  )
}
